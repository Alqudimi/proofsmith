## Summary

Describe the problem and the invariant this change preserves or introduces.

## Evidence

- [ ] Relevant unit or integration tests added.
- [ ] `PYTHONPATH=src pytest` passes.
- [ ] `ruff check src tests` passes.
- [ ] `mypy src` passes when Python code changes.
- [ ] `pnpm check` and `pnpm build` pass when frontend changes.

## Security and compatibility

Describe any new input, process execution, filesystem, network, dependency, or evidence-redaction boundary. Mention migration or compatibility impact explicitly.
